# Production builds: empty so the bundled JS uses relative URLs
# (calls /health, /samples, /generate, /generate/stream on the same origin
# as the page — which is whatever host serves the static files).
VITE_API_BASE=
